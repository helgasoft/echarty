# Workflow for building and uploading a WebAssembly R package image to a GitHub Release
# Adapted from: https://github.com/r-wasm/actions/blob/main/.github/workflows/release-file-system-image.yml
name: Build and release WebAssembly image

on:
  # Trigger this workflow when you publish a new GitHub Release
  release:
    types: [published]

  # Allow manual triggering from the Actions tab for testing
  workflow_dispatch:
    inputs:
      packages:
        description: 'R packages to include (space or comma separated). Defaults to "." (current package)'
        required: false
        default: '.'
        type: string
      strip:
        description: 'Directories to strip from the image (e.g., "demo, doc, examples"). Use "NULL" for none.'
        required: false
        default: 'demo, man, pkgdown, tests, vignettes, doc, examples, help, html'
        type: string
      webr-image:
        description: 'webR Docker image to use for building'
        required: false
        default: 'ghcr.io/r-wasm/webr:main'
        type: string
      compress:
        description: 'Compress the Emscripten VFS image'
        required: false
        default: false
        type: boolean

# Allow only one concurrent build for a given release tag
concurrency:
  group: ${{ github.workflow }}-${{ github.event.release.tag_name || github.run_id }}
  cancel-in-progress: true



# Simplified version with hardcoded options
jobs:
  build-and-upload:
    runs-on: ubuntu-24.04
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Test GitHub authentication
        uses: addnab/docker-run-action@v3
        with:
          image: ghcr.io/r-wasm/webr:main
          options: -v ${{ github.workspace }}:/work -w /work
          run: |
            Rscript -e "
              # Test GitHub API access
              result <- system2('curl', c('-H', paste0('Authorization: token ', Sys.getenv('GITHUB_PAT')), 
                                   'https://api.github.com/rate_limit'), stdout = TRUE)
              cat(paste(result, collapse = '\n'))
            "
          
      - name: Build wasm image using webR Docker container
        uses: addnab/docker-run-action@v3
        env:
          GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
        with:
          image: ghcr.io/r-wasm/webr:main
          options: |
            -v ${{ github.workspace }}:/work
            -w /work
            -e GITHUB_PAT=${{ secrets.GITHUB_TOKEN }}
          run: |
            Rscript -e "
              # Set GitHub PAT for authentication
              Sys.setenv(GITHUB_PAT = Sys.getenv('GITHUB_PAT'))
              
              library(rwasm)
              
              # Step 1: Add the current package to the repository
              message('Step 1: Building package for WebAssembly...')
              rwasm::add_pkg(
                packages = 'local::.',
                repo_dir = './repo',
                dependencies = NA,  # Install hard dependencies
                remotes = NA  # Prefer built-in package list
              )
              
              # Step 2: Create the filesystem image
              message('Step 2: Creating filesystem image...')
              rwasm::make_vfs_library(
                repo_dir = './repo',
                out_dir = './vfs',
                out_name = 'library'
              )
              
              message('✓ Build completed successfully')
              print(list.files('./vfs', recursive = TRUE))
            "
